import React from 'react'
import { View, Text } from 'react-native'

const CollectionsOverview = ({item}) => {
    const {title}
    return (
        <View>
            <Text></Text>
        </View>
    )
}

const styles = StyleSheet.create({
    style
})

export default CollectionsOverview
